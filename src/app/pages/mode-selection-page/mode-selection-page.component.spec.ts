import { TestBed } from '@angular/core/testing';
import { ModeSelectionPageComponent } from './mode-selection-page.component';

describe('ModeSelectionPageComponent', () => {
  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [
        ModeSelectionPageComponent
      ],
    }).compileComponents();
  });


});
