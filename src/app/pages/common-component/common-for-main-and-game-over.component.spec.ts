import { TestBed } from '@angular/core/testing';
import { CommonForMainAndGameOverComponent } from './common-for-main-and-game-over.component';

describe('CommonForMainAndGameOverComponent', () => {
  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [
        CommonForMainAndGameOverComponent
      ],
    }).compileComponents();
  });


});
