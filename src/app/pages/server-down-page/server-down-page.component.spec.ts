import { TestBed } from '@angular/core/testing';
import { ServerDownPageComponent } from './game-over-page.component';

describe('GameOverPageComponent', () => {
  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [
        ServerDownPageComponent
      ],
    }).compileComponents();
  });


});
